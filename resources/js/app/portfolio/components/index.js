import Vue from 'vue'

export const PortfolioIndex = Vue.component('portfolio-index', require('./PortfolioIndex.vue').default)
export const Wallet = Vue.component('wallet', require('./Wallet.vue').default)